name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *'  

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s https://formulario-oi3p.onrender.com
